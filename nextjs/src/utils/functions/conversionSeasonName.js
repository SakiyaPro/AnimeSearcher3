export function conversionSeasonName(name) {
    if (name === "WINTER") {
        return "冬"
    }
    else if (name === "SPRING") {
        return "春"
    }
    else if (name === "SUMMER") {
        return "夏"
    }
    else {
        return "秋"
    }
}
